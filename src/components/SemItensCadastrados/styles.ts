import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 0.5,
        justifyContent: "center",
        alignItems: "center"
    },
    imagem: {
        width: 80,
        height: 80,
        marginBottom: 30
    },
    text: {
        fontSize: 17,
        color: "#6B6B6B"
    }
});